
import React from "react";

const Cart=(props)=>{
    return (
        <div>
            <h3>{props.name}</h3>
            <p>{props.des}</p>
        </div>
    );
}

export default Cart;